# Upstream refresh — September 14, 2026

The sanitized publication commit was rebased onto 20 upstream commits, from
`6f39a47bb3` to `5fb3b7e401`. No textual merge conflicts occurred. The publication
branch keeps public upstream ancestry and one custom commit, without the earlier
custom development history.

## Upstream changes

- Correct clipboard routing through tmux and SSH, tab preservation during paste,
  and fuzzy matching within Unicode lowercase expansions.
- Cancel code-mode timer tasks when cleared or when their cell finishes.
- Preserve Guardian parent-response metadata and invalidate stale review sessions;
  extract shared reviewer bookkeeping and transcript selection.
- Share MCP catalogs and output schemas, improve enterprise registration lookup,
  and include session/originating-window metadata in MCP calls.
- Add dedicated managed-network-proxy listeners and improve rollout compression
  diagnostics, Windows setup helpers, and provisioned macOS release packaging.

## Custom-feature and prompt audit

The configured base prompt and custom prompt templates are unchanged by these
upstream commits. Delegation controls, bundle APIs, math rendering, Live Transcribe
dictation, monitors, and Reserve refusal remain. Overlapping tool-definition changes
adopt upstream's shared output-schema representation without replacing prompt text.

The sanitized examples and notes are retained. No private development commits or
local configuration are included in the publication branch.

## Validation

- Updated CLI, exec, test-client, and code-mode host executables built successfully.
- All 5,705 selected regression checks passed across the TUI, Core's code-mode,
  bundle, Guardian, and multi-agent paths, protocol schemas, code-mode runtime,
  shared reviewer, and tool definitions. One known cold-resume timing test passed
  on retry. The complete workspace suite was not run.
- Stable/experimental API and config schema regeneration passed. The Bazel lock
  refresh passed with non-fatal upstream dependency advisories.
- Personal-path, example, and ancestry checks confirm the sanitized publication
  tree and single-custom-commit history remain intact.
- Scoped Rust linting passed for Core, TUI, app-server protocol, code-mode runtime,
  shared Guardian reviewer, and tool definitions.
